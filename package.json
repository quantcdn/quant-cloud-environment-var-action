{
  "name": "quant-cloud-environment-var-action",
  "version": "1.0.0",
  "description": "GitHub Action for managing environment variables in Quant Cloud",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts",
    "test": "jest",
    "prepare": "husky"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@quantcdn/quant-client": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.38.3",
    "husky": "^9.1.7",
    "typescript": "^5.0.0"
  }
}
